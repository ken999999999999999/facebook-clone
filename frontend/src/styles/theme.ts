import { createTheme } from "@mui/material/styles"

const theme = createTheme({
  palette: {
    background: {
      default: "#f1f2f5",
    },
    primary: {
      main: "#4267B2",
    },
    secondary: {
      main: "#898F9C",
    },
  },
})

export default theme
